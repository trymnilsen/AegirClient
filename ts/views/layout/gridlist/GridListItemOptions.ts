module App.View.Layout.Grid {

    export interface GridListItemOptions {
        /**
         * Hex string representing color
         */
        background?: string;

        padding?: string;

        margin?: string;
    }
}